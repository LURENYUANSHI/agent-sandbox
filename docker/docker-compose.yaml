version: "3.9"

services:
  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - sandbox-data:/app/data
    environment:
      - SANDBOX_DATA_DIR=/app/data
      - SANDBOX_CONFIG_DIR=/app/configs

volumes:
  sandbox-data:
